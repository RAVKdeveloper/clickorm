export const DEFAULT_ENTITIES_PATH = `${process.cwd()}/**/*.{js,ts}`
export const DEFAULT_DB_CONNECTION_NAME = 'default'
export const DEFAULT_MIGRATIONS_PATH = `migrations`
export const DEFAULT_DATA_SOURCE_NAME = 'clickorm.datasource'
